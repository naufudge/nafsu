import React from 'react'

const Form = ({show}) => {
    const showForm = () => {
        document.getElementById("my_modal").showModal();
    }

    return (
        <></>
    )
}

export default Form;